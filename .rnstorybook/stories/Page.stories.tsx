import type { Meta } from '@storybook/react-native-web-vite';

import { Page } from './Page';

export default {
  title: 'Example/Page',
  component: Page,
} as Meta<typeof Page>;

export const Default = {};
